/**
 * Core application structure components.
 *
 * These components define the application shell, bootstrap lifecycle,
 * and top-level content composition. They are framework-agnostic and
 * must not depend on routing, permissions, or business logic.
 */
export { default as AppShell } from './AppShell';
export { default as AppBootstrapGate } from './AppBootstrapGate';
export { default as AppContent } from './AppContent';
