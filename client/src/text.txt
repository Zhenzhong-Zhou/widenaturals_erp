feat: initialize Vite + React + TypeScript project with Material UI and global styles

- Setup Vite with React and TypeScript template
- Install Material UI, Emotion, and React Router
- Add `theme.tsx` for Material UI theme configuration
- Create `styles/global.css` for global resets and typography
- Configure routing in `App.tsx` with lazy loading for pages
- Add initial directory structure:
  - `src/components/`: Reusable components
  - `src/pages/`: Page components
  - `src/styles/`: Global and shared styles
  - `src/routes/`: Centralized route configuration
- Include `.env` support for environment variables

