# Application default configuration
#
# This file defines safe, non-secret defaults that describe the
# intended behavior of the application.
#
# These values are the lowest-precedence configuration and may be
# overridden by environment-specific files (e.g. server or database envs).
#
# Do NOT put secrets, credentials, or machine-specific values here.

# =====================
# Core defaults
# =====================
NODE_ENV=development
PORT=8081
API_PREFIX=/api/v1

# =====================
# Logging
# =====================
LOG_LEVEL=debug
LOGS_DIR=./logs
LOG_IGNORED_ROUTES=/health,/status,/metrics,/favicon.ico

# =====================
# Backups
# =====================
BACKUP_DIR=./backups
MAX_BACKUPS=30

# =====================
# CORS defaults
# =====================
ALLOWED_HEADERS=Content-Type,Authorization,X-CSRF-Token,X-Device-Id
ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
EXPOSED_HEADERS=Content-Disposition
ALLOW_CREDENTIALS=true
OPTIONS_SUCCESS_STATUS=204

# =====================
# Cookies / CSRF defaults
# =====================
CSRF_TESTING=false
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

# =====================
# Rate limiting
# =====================
RATE_LIMIT_WINDOW_MS=50000
RATE_LIMIT_MAX=200
