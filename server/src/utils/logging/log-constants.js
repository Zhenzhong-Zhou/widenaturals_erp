/**
 * @file log-constants.js
 * @description Shared logging constants to avoid circular dependencies.
 */

const LOG_LEVELS = ['debug', 'info', 'warn', 'error', 'fatal'];

module.exports = {
  LOG_LEVELS,
};
